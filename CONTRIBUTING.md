# Contributing to MrNothing Studio

## Setup
```bash
npm install && cp .env.example .env.local && npm run dev
```

## Commit Convention
```
feat(builder): add personality configuration step
feat(engine): implement tool-use agent loop
fix: resolve system prompt generation edge case
test: add agent build validation tests
```
